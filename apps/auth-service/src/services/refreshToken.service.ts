import { RefreshTokenInput } from '../types/RefreshTokenInput';
// import { RefreshTokenOutput } from "../types/RefreshTokenOutput";

export const refreshToken = async ({ token }: RefreshTokenInput) => {
  return token;
};
